<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="im.flos.webapp.api.member.mapper.MemberMapper">
    <select id="selectById" resultType="im.flos.webapp.api.member.persistence.MemberDTO">
        SELECT member.id, member.short_name, member.full_name, member.hash
        FROM member
        WHERE id = #{id}
    </select>
    <select id="selectByName" resultType="im.flos.webapp.api.member.persistence.MemberDTO">
        SELECT member.id, member.short_name, member.full_name, member.hash
        FROM member
        WHERE short_name = #{name}
    </select>
    <select id="selectAll" resultType="im.flos.webapp.api.member.persistence.MemberDTO">
        SELECT member.id, member.short_name, member.full_name, member.hash
        FROM member
    </select>
</mapper>